+        <div className={state.zone === "floor555" ? "strip-current" : ""}><span className="strip-label">Ниже</span><strong>Этаж 555</strong><small>ремонтный пояс</small></div>
+        <span className="strip-connector">⇅</span>
+        <div className={state.zone === "floor556" ? "strip-current" : ""}><span className="strip-label">Опорный</span><strong>Этаж 556</strong><small>технический пояс</small></div>
+        <span className="strip-connector">⇅</span>
+        <div className={state.zone === "floor557" ? "strip-current" : ""}><span className="strip-label">Выше</span><strong>Этаж 557</strong><small>жилой контур</small></div>
         <span className="strip-connector">⇄</span>
         <div className={state.zone === "voidLab" ? "strip-current void" : ""}><span className="strip-label">Межэтажье</span><strong>ВЖ-7</strong><small>лабораторный слой</small></div>
       </section>
@@ -669,7 +680,9 @@ export default function GamePrototype() {
                         ? "#7ea59d"
                         : tile === "P" && known
                           ? "#b06bdd"
-                          : "#252a26";
+                          : (tile === "U" || tile === "D") && known
+                            ? "#82b7ad"
+                            : "#252a26";
 
                 return (
                   <g
@@ -691,11 +704,11 @@ export default function GamePrototype() {
                       opacity={visible ? 1 : known ? 0.5 : 0.78}
                       stroke={stroke}
                       strokeWidth={inTargetAggro ? 2.8 : inTargetVision || inWeaponRange ? 2 : 1.3}
-                      filter={tile === "P" && visible ? "url(#void-glow)" : undefined}
+                      filter={(tile === "P" || tile === "U" || tile === "D") && visible ? "url(#void-glow)" : undefined}
                     />
                     {marker ? (
                       <g opacity={visible ? 1 : 0.55}>
-                        <circle cx={cx} cy={cy + TILE_HEIGHT / 2 - 3} r={tile === "P" ? 13 : 10} fill="#20231f" stroke={tile === "A" ? "#e4c170" : "#aeb4a7"} strokeWidth="2" />
+                        <circle cx={cx} cy={cy + TILE_HEIGHT / 2 - 3} r={tile === "P" || tile === "U" || tile === "D" ? 13 : 10} fill="#20231f" stroke={tile === "A" ? "#e4c170" : "#aeb4a7"} strokeWidth="2" />
                         <text x={cx} y={cy + TILE_HEIGHT / 2 + 2} textAnchor="middle" className="map-symbol">{marker}</text>
                       </g>
                     ) : null}
diff --git a/docs/STATUS.md b/docs/STATUS.md
index dc97dbe..44efcb1 100644
--- a/docs/STATUS.md
+++ b/docs/STATUS.md
@@ -5,6 +5,7 @@
 ## Реализовано в доступном коде
 
 - этапы 1, 2, 3A, 3B и 3C;
+- этап 4A: связанные этажи 555, 556 и 557 с переходами непосредственно на карте;
 - непрерывная локальная симуляция;
 - бой, ИИ, экипировка, добыча, травмы и артефакты;
 - дерево развития и активные навыки;
@@ -14,8 +15,7 @@
 
 ## Спроектировано, но отсутствует в найденном архиве исходников
 
-- пять связанных протяжённых этажей;
-- переходы вверх и вниз непосредственно на карте;
+- расширение текущих трёх этажей до пяти протяжённых этажей;
 - пять геномных популяций с фоновой симуляцией;
 - четыре масштаба поселений;
 - четыре автономных босса с личными целями;
@@ -24,4 +24,4 @@
 - задания от города-патрона;
 - серверный общий снимок мира.
 
-Эти механики описаны в `GAME_DESIGN.md` как этап 4, однако найденный архив называется `stage3c` и не содержит соответствующих сущностей в игровом ядре. Поэтому репозиторий честно маркирован как исходный снимок этапа 3C, а этап 4 перенесён в ближайший план разработки.
+Эти механики описаны в `GAME_DESIGN.md` как этап 4. Первый вертикальный срез этапа 4 уже начат: этажи 555, 556 и 557 связаны переходами вверх и вниз, позиции героя на каждом этаже сохраняются, а на соседних этажах действуют собственные противники.
diff --git a/tests/game-engine.test.mjs b/tests/game-engine.test.mjs
index acfb9d0..90dbd48 100644
--- a/tests/game-engine.test.mjs
+++ b/tests/game-engine.test.mjs
@@ -19,7 +19,9 @@ import {
   effectiveInjury,
   equipItem,
   equipWeapon,
+  FLOOR_555_MAP,
   FLOOR_MAP,
+  FLOOR_557_MAP,
   hasLineOfSight,
   heroAttackRange,
   heroDefense,
@@ -48,7 +50,9 @@ function heroAt(state, point) {
 }
 
 test("карты прямоугольные, а базовое и легендарное оружие задают разные дистанции боя", () => {
+  assert.equal(new Set(FLOOR_555_MAP.rows.map((row) => row.length)).size, 1);
